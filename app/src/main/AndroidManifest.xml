<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <uses-permission
        android:name="android.permission.INTERNET"/>

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity_business_cat"/>
        <activity android:name=".activity_business"
            android:theme="@style/category"/>
        <activity android:name=".activity_profile" />
        <activity android:name=".activity_login" />
        <activity android:name=".activity_category" />
        <activity
            android:name=".activity_saved"
            android:theme="@style/category" />
        <activity
            android:name=".detailed_info"
            android:theme="@style/category" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity_vet"
            android:theme="@style/category" />
        <activity
            android:name=".activity_pension"
            android:theme="@style/category" />
        <activity
            android:name=".activity_salon"
            android:theme="@style/category" />
        <activity
            android:name=".activity_walker"
            android:theme="@style/category" />
        <activity android:name=".activity_registration" />
    </application>

</manifest>